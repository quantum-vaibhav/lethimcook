# the song is downloaded per-machine by setup.py — don't redistribute it
thinking-song.mp3

# python
__pycache__/
*.pyc

# claude code local state
.claude/settings.local.json

# lethimcook master ON/OFF switch (per-machine runtime state)
.lethimcook-active
